id: BASE_6726
sourceId: zdb-baseline-20260526
enabled: true
severity: MANDATORY
targetTable: YX_TRCY_TB
title: 32号记录植被覆盖为空
explanation: 来源于 ZDB 内置启用规则：32号记录植被覆盖为空。
requiredTables:
  - YX_TRCY_TB
requiredFields:
  - YD_ID
  - YX_ID
locatorFields:
  - YD_ID
  - YX_ID
sql: |-
  SELECT * FROM "YX_TRCY_TB" WHERE YD_ID = :ydId AND (YX_CD=40 AND (ZCJL_ZBFG_32 IS NULL OR ZCJL_ZBFG_32 ='' OR ZCJL_ZBFG_32=' ' ))
