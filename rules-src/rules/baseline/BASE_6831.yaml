id: BASE_6831
sourceId: zdb-baseline-20260526
enabled: true
severity: MANDATORY
targetTable: YF_TRCY_TB
title: 缺少高大草灌植物调查表
explanation: 来源于 ZDB 内置启用规则：缺少高大草灌植物调查表。
requiredTables:
  - YF_TRCY_TB
  - ZWDCB_GDCG_TB
requiredFields:
  - YD_ID
  - YF_ID
locatorFields:
  - YD_ID
  - YF_ID
sql: |-
  SELECT * FROM "YF_TRCY_TB" WHERE YD_ID = :ydId AND (MZGUID NOT IN (SELECT XB_GLH FROM ZWDCB_GDCG_TB WHERE XB_GLH IS NOT NULL ))
