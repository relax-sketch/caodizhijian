id: BASE_6640
sourceId: zdb-baseline-20260526
enabled: true
severity: MANDATORY
targetTable: YD_TRCY_PT
title: 缺少测产样方表
explanation: 来源于 ZDB 内置启用规则：缺少测产样方表。
requiredTables:
  - YD_TRCY_PT
  - YF_TRCYCC_TB
requiredFields:
  - YD_ID
  - XIAN
  - XIANG
  - CUN
locatorFields:
  - XIAN
  - XIANG
  - CUN
  - YD_ID
sql: |-
  SELECT * FROM "YD_TRCY_PT" WHERE YD_ID = :ydId AND (MZGUID NOT IN (SELECT XB_GLH FROM YF_TRCYCC_TB WHERE XB_GLH IS NOT NULL))
